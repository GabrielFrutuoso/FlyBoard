export type SelectedKey = { rowIndex: number; keyIndex: number };

export type SelectedKeys = SelectedKey[];

export type PaletteMode = "add" | "replace";

export type DropTarget = { rowIndex: number; index: number };
